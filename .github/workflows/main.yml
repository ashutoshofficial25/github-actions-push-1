name: push trigger demo

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show message
        run: echo "Workflow started"

      - name: List files
        run: ls -ltr

      - name: Read app.txt
        run: cat app.txt



        
